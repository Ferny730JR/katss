add_executable(ikke ikke.c ikke_cmdl.c)
target_link_libraries(ikke kkctr_static KATSS_STRING)
if(ipo_is_supported)
    set_property(TARGET ikke PROPERTY INTERPROCEDURAL_OPTIMIZATION_RELEASE ON)
else()
    target_compile_options(ikke "-O3")
endif()

add_executable(test test.c)
target_link_libraries(test kkctr_static)
if(ipo_is_supported)
    set_property(TARGET test PROPERTY INTERPROCEDURAL_OPTIMIZATION_RELEASE ON)
endif()

add_executable(test_mt test_mt.c)
target_link_libraries(test_mt kkctr_static)
if(ipo_is_supported)
    set_property(TARGET test_mt PROPERTY INTERPROCEDURAL_OPTIMIZATION_RELEASE ON)
endif()
